# CORS configuration
# Comma-separated list of allowed origins
# If not set, defaults to http://localhost:3000,http://172.20.10.2:3000
CORS_ORIGINS=http://localhost:3000,http://example.com

# Application host (used by uvicorn)
APP_HOST=0.0.0.0

# Application port (used by uvicorn)
APP_PORT=8000
